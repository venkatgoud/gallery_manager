<h1>New Photo for <%= @username %> </h1>

<%= render "form.html", Map.put(assigns, :action, Routes.user_photo_path(@conn, :create, @user_id)) %>

<span><%= link "Back", to: Routes.user_photo_path(@conn, :index, @user_id) %></span>
